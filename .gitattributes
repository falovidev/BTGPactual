# Normalize line endings
* text=auto

# Shell scripts must use LF
*.sh text eol=lf
gradlew text eol=lf

# Windows batch files must use CRLF
*.bat text eol=crlf

# Binary files
*.jar binary
*.zip binary
